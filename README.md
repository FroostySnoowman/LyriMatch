# LyriMatch

`cd backend && python3 -m venv venv && source venv/bin/activate && pip3 install --upgrade pip && pip3 install -r requirements.txt`
`cd backend && python3 app.py`

## Dataset
The dataset lives in
`~/.cache/kagglehub/`